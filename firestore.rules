rules_version = '2';
// DEV / coursework only — tighten before any public release.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
